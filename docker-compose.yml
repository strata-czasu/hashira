version: '3.8'

services:
  app:
    build: .
    restart: unless-stopped
    depends_on:
      - db
    networks:
      - db

  db:
    image: postgres:latest
    restart: unless-stopped
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - db

volumes:
  postgres-data:

networks:
  db: